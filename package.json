{
    "name": "clipcc-extension-em-pip",
    "version": "1.0.0",
    "author": "Edit Mr.",
    "scripts": {
        "build": "rimraf ./build && mkdirp build && rimraf ./dist && mkdirp dist && webpack --bail",
        "build:dist": "NODE_ENV=production yarn run build"
    },
    "dependencies": {
        "clipcc-extension": "^0.2.0"
    },
    "devDependencies": {
        "copy-webpack-plugin": "^11.0.0",
        "mkdirp": "^1.0.4",
        "rimraf": "^3.0.2",
        "webpack": "^5.75.0",
        "webpack-cli": "^5.0.1",
        "zip-webpack-plugin": "^4.0.1"
    }
}
